apiVersion: v1
kind: Service
metadata:
  name: {{ include "eurooffice.fullname" . }}
  labels:
    {{- include "eurooffice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "eurooffice.selectorLabels" . | nindent 4 }}
